//===----------------------------------------------------------------------===//
//
// This source file is part of the Swift open source project
//
// Copyright (c) 2025 Apple Inc. and the Swift project authors
// Licensed under Apache License v2.0 with Runtime Library Exception
//
// See http://swift.org/LICENSE.txt for license information
// See http://swift.org/CONTRIBUTORS.txt for the list of Swift project authors
//
//===----------------------------------------------------------------------===//

(
    {
        Domain = webassembly;
        Type = Architecture;
        Identifier = Standard;
        Name = "Standard Architectures";
        Description = "Standard architectures";
        ArchitectureSetting = "ARCHS_STANDARD";
        RealArchitectures = (
            "$(VALID_ARCHS)"
        );
        SortNumber = 0;
    },
    {
        Domain = webassembly;
        Type = ProductType;
        Identifier = org.swift.product-type.common.object;
        BasedOn = com.apple.product-type.library.static;
    },
    // Subspecs under `Domain = webassembly` MUST declare qualified
    // `BasedOn = webassembly:...` references. `BasedOn` is resolved
    // from the requesting domain's perspective, so an unqualified
    // `BasedOn = com.apple.product-type.tool` in a `webassembly`
    // subspec can be silently shadowed when a descendant subdomain
    // (wasi, emscripten) registers an override with the same
    // identifier.
    {
        Domain = webassembly;
        Type = ProductType;
        Identifier = com.apple.product-type.tool;
        BasedOn = default:com.apple.product-type.tool;
        DefaultBuildProperties = {
            EXECUTABLE_SUFFIX = ".$(EXECUTABLE_EXTENSION)";
            EXECUTABLE_EXTENSION = "wasm";
        };
    },
    {
        Domain = webassembly;
        Type = ProductType;
        Identifier = com.apple.product-type.tool.host-build;
        BasedOn = webassembly:com.apple.product-type.tool;
    },
    {
        Domain = webassembly;
        Type = ProductType;
        Identifier = com.apple.product-type.bundle.unit-test;
        BasedOn = org.swift.product-type.common.object;
        DefaultBuildProperties = {
            ENABLE_TESTING_SEARCH_PATHS = YES;
            // Index store data is required to discover XCTest tests
            COMPILER_INDEX_STORE_ENABLE = YES;
            SWIFT_INDEX_STORE_ENABLE = YES;
            // Testability is needed to generate code to invoke discovered XCTest tests
            SWIFT_ENABLE_TESTABILITY = YES;
            GENERATE_TEST_ANCHOR = YES;
        };
    },
    {
        Domain = webassembly;
        Type = ProductType;
        Identifier = com.apple.product-type.tool.swiftpm-test-runner;
        BasedOn = default:com.apple.product-type.tool.swiftpm-test-runner;
        DefaultBuildProperties = {
            EXECUTABLE_SUFFIX = ".$(EXECUTABLE_EXTENSION)";
            EXECUTABLE_EXTENSION = "wasm";
        };
    },
    {
        Domain = emscripten;
        Type = ProductType;
        Identifier = com.apple.product-type.tool;
        BasedOn = webassembly:com.apple.product-type.tool;
        DefaultBuildProperties = {
            EXECUTABLE_EXTENSION = "js";
        };
    },
    {
        Domain = emscripten;
        Type = ProductType;
        Identifier = com.apple.product-type.tool.swiftpm-test-runner;
        BasedOn = webassembly:com.apple.product-type.tool.swiftpm-test-runner;
        DefaultBuildProperties = {
            EXECUTABLE_EXTENSION = "js";
        };
    },
)
